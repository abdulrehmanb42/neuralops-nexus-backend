# ── Web Framework & Server ────────────────────────────────────────────────────
fastapi[standard]
pydantic-settings
uvicorn[standard]

# ── AI Agent Framework ────────────────────────────────────────────────────────
# openai extra = OpenAI / OpenAI-compatible providers
# anthropic extra = Anthropic native provider (pydantic-ai 2.x; litellm extra dropped)
pydantic-ai-slim[openai,mcp,anthropic]

# ── LLM + Embedding Provider Routing ─────────────────────────────────────────
# Single gateway for all LLM calls (Anthropic, OpenAI, Azure, Ollama, etc.).
# pydantic-ai → LiteLLM → provider. Swap provider via model_id string only.
litellm

# ── Local Embedding (ONNX, runs inside nexus-ai) ──────────────────────────────
# FastEmbed runs nomic-embed-text-v1.5 via ONNX — no GPU, no network.
# Switch to LiteLLM embedding (Ollama/Infinity) via EMBEDDING_PROVIDER=litellm.
fastembed
numpy                    # Used by M7 cosine similarity classifier (also a fastembed dep)

# ── Vector Store ──────────────────────────────────────────────────────────────
# chromadb intentionally NOT installed -- dropped to cut image size
# (fastembed's onnxruntime is already the big line item; chromadb's own
# dependency tree was adding more on top). chroma_store.py and the
# "chroma" branch in VectorStoreFactory are still in the codebase but are
# now dead/unreachable unless chromadb is reinstalled -- pgvector is the
# default and only working backend as of this image.
asyncpg                  # pgvector backend -- direct async Postgres client
pgvector                 # registers the VECTOR column type/codec with asyncpg

# ── Dev & Testing ─────────────────────────────────────────────────────────────
rich                     # Better logging
pytest
pytest-asyncio
